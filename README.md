msbuild-api-hacks
=================

Just a little bit of fiddling with the MsBuild API.
MsBuild is the new tool that eventually replaced NMake for performing automated builds for VisualStudio projects.
The approach presented here shows how the MsBuild platform can be accessed programmatically rather than using command-line tools.
